@model Proyecto_PetSupply.Models.Horario

@{
    ViewBag.Title = "Details2";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<style>
    h2 {
        text-align: center;
    }

    .product-details {
        max-width: 500px;
        margin: 0 auto;
    }

    .btn-group {
        text-align: center;
        margin-top: 20px;
    }

        .btn-group a {
            margin-right: 10px;
        }
</style>

<h2>Detalles</h2>

<div>
    <h4>Datos del horario</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Id
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Id)
        </dd>
        <dt>
            Dia
        </dt>

        <dd>
            @ViewBag.Horario
        </dd>

        <dt>
            Hora
        </dt>

        <dd>
            @Html.DisplayFor(model => model.hora)
        </dd>

    </dl>
</div>
<p>
    <a href="@Url.Action("Edit2", new { id = Model.Id })" class="btn btn-success">Editar</a> |
    <a href="@Url.Action("Delete2", new { id = Model.Id })" class="btn btn-danger">Eliminar</a>
    <br />
    <br />
    @Html.ActionLink("Regresar a lista de Horarios", "Index2")
</p>
